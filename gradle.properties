org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false

## Environment Properties
minecraft_version=1.20.1
minecraft_version_range=[1.20.1,1.21)
forge_version=47.4.10
forge_version_range=[47,)
loader_version_range=[47,)

mapping_channel=official
mapping_version=1.20.1

## Mod Properties
mod_id=appliedember
mod_name=AppliedEmber
mod_license=GNU GPL 3.0
mod_version=0.1.7
mod_group_id=com.pingsu
mod_authors=pingsu
mod_description=Applied Energistics 2 与 Embers Rekindled 的联动：ME 余烬元件（ME Ember Cell），把余烬作为 ME 网络中的一种 AEKey 进行存储、导入与导出。

## Dependency Versions (pack-provided)
ae2_version=15.4.10
embers_version=7279674
## Optional compat: MEGA Cells (file id on CurseMaven). The mega ember cell line is only registered
## when the mod is loaded; still shipped to the dev runs so datagen can resolve its textures.
megacells_version=6175008
## MEGA Cells declares Cloth Config as a mandatory dependency; only needed on the dev runs, like
## GuideME. CurseMaven file id of cloth-config-11.1.106-forge (1.20.1).
clothconfig_version=4633444
## AE2 15.4.x requires GuideME at runtime; only needed for the dev runs (datagen), not for compiling.
guideme_version=20.1.15

## Test dependencies
junit_version=5.10.2
junit_platform_version=1.10.2
