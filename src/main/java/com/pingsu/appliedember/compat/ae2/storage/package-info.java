/**
 * Adapters between AE2's generic storage inventories and Embers' capability model: the read-only
 * {@link com.pingsu.appliedember.compat.ae2.storage.IGridEmberView} contract (plan B4), its
 * {@code GenericStackEmberStorage} adapter and the capability registration that publishes them
 * (plan 4.2/4.5, plan §7 target structure).
 */
package com.pingsu.appliedember.compat.ae2.storage;
